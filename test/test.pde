// 测试已迁至 new_state：所有参数在正式配置中设置，此处不再维护。
// 使用方式：打开 new_state 草图，在 new_state.pde 顶部将 TEST_MODE 设为 true，运行即可。
// 按键 1–0 对应：跳、马上有对象、马上有钱、马上有福、马上起飞、跨过坎坷、跨过阻碍、跨过了迷茫、马到成功、马年大吉(起扬)。

void setup() {
  size(400, 80);
  background(240);
  textAlign(LEFT, CENTER);
  textSize(14);
  fill(0);
  text("请打开 new_state 草图，将 new_state.pde 中 TEST_MODE 改为 true 后运行。", 20, 40);
}

void draw() {
}
